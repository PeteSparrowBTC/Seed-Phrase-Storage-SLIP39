{
  "name": "descriptor-crosscheck",
  "private": true,
  "description": "Independent expectations for the computed multisig descriptor and first receive address, from @scure/bip32 and @scure/btc-signer. Deliberately independent of Slip39Demo.Core so the C# derivation cannot vouch for itself. Kept out of tools/independent-verify so the pinned verifier bundle CI checks stay untouched.",
  "type": "module",
  "scripts": {
    "crosscheck": "node crosscheck.mjs"
  },
  "dependencies": {
    "@scure/base": "1.2.6",
    "@scure/bip32": "1.7.0",
    "@scure/bip39": "1.6.0",
    "@scure/btc-signer": "1.8.1"
  }
}
